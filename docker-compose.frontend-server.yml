services:
  frontend:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./web:/usr/share/nginx/html:ro
      - ./deploy/regcloud/nginx.frontend-local-backend.conf:/etc/nginx/conf.d/default.conf:ro
